using System;
using UnityEngine;

/*
/// <summary>
/// Attribute used to draw debug information in scene view.
/// </summary>
[AttributeUsage(AttributeTargets.Field)]
public class SVDebug : Attribute
{
    public Color Color;

    public SVDebug()
    {
        Color = Color.white;
    }

    public SVDebug(float r, float g, float b)
    {
        Color = new Color(r, g, b);
    }

    public SVDebug(float r, float g, float b, float a)
    {
        Color = new Color(r, g, b, a);
    }
}
*/